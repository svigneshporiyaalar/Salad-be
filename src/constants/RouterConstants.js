const API = '/api';
const V1 = '/v1';

module.exports = {
  v1: {
    auth: `${API}${V1}/auth`,
    userService: `${API}${V1}/userService`,
    partnerService: `${API}${V1}/partnerService`,
    goal: `${API}${V1}/goal`,
    badge: `${API}${V1}/badge`,
    userOnboard: `${API}${V1}/userOnboard`,


  },
};
